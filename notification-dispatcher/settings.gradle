plugins {
	// Auto-provisions the Java 21 toolchain via the Foojay Disco API, so the build does
	// not depend on which JDK the host has installed (this machine only has JDK 17).
	id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'notification-dispatcher'
